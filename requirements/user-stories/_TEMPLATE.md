# US-XXX — <short title>

**Status:** proposed | accepted | in-dev | in-review | done | deferred
**Owner role:** ui-developer | backend-developer | (multiple)
**Created:** YYYY-MM-DD
**Story ID:** US-XXX

---

## Narrative

As a `<role/persona>`, I want to `<action>`, so that `<benefit/outcome>`.

## Acceptance Criteria

- **AC1:** Given `<context>`, when `<action>`, then `<observable outcome>`. _(Testable: yes/no assertion)_
- **AC2:** Given `<context>`, when `<action>`, then `<observable outcome>`.
- **AC3:** …

_Every AC must be independently verifiable. If an AC contains "appropriate", "reasonable", or "as needed" — quantify it before marking the story `accepted`._

## Out of Scope

- List anything that a reader might assume is in scope but isn't.
- Defer intentionally: "Not included because X; tracked as OQ-NNN / future story."

## Open Questions

- Link to `open-questions.md` entries if an AC depends on an unresolved question.
- If all questions are resolved, delete this section.

## Design Spec

- Link to `<workspace>/design/<spec>.md` if this story has a UI component. UX Designer owns this link.
- Non-UI stories: remove this section.

## Links

_(Filled in during and after implementation)_

- impl: `<commit SHA>`
- test: `<test file path>`
- design-pass-by: `<ux-designer HANDOFF or commit ref>`
- qa-pass-by: `<qa HANDOFF or commit ref>`
- deployed-by: `<devsecops commit>`
