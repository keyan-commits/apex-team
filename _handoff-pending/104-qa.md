## Done
- PR #283 revised: replaced `vi.doMock`+`vi.resetModules` race with single hoisted `vi.mock` + per-test `mockImplementation`
- Verified 422/422 tests pass consistently across 3 consecutive runs
- Type-check: 0 errors
- Ready for architect re-gate before merge

## In flight
- Awaiting architect PASS on #283

## Next
- Push #283 → architect re-gates → merge #283
- Then re-update #278 → confirm both build jobs green → merge #278

## Notes
- BR-006 semantics unchanged — opus-override and no-override assertions identical
- Root cause: `vi.resetModules()` clears module cache but NOT `vi.doMock` registrations, causing fork-pool resolver races
- Solution deterministic: one registration + per-test behavior control via `mockImplementation`
