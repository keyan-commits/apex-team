## Done
- Removed `claude-opus-4-7` from `KNOWN_MODELS` in `AgentPane.tsx` and `dashboard/page.tsx` — syncs both lists with `pricing.ts` MODEL_PRICING keys (closes #218, #224).

## In flight
- PR #224 open, awaiting Architect gate (UX confirmed no visual surface — data-list sync only).

## Next
- Wave 100 (lfm-b2b Done panel grouping + max-height) after #224 merges.

## Notes
- `claude-opus-4-8` retained; `claude-opus-4-7` was the only retired entry.
- Type-check 0, build clean.
